fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
httpx==0.27.2
python-dotenv==1.0.1
google-genai==0.3.0
openai==1.51.0
mutagen==1.48.1

# Diarization (item 1): VAD turn-boundaries + speaker-embedding clustering.
# webrtcvad-wheels ships prebuilt Windows wheels for the same webrtcvad C
# extension (plain `webrtcvad` fails to build on Windows without MSVC Build
# Tools). resemblyzer itself is installed separately with --no-deps — see
# requirements-diarization-nodeps.txt and README setup instructions — since
# its own dependency list pins plain `webrtcvad` and would otherwise try
# (and fail) to build it from source.
webrtcvad-wheels==2.0.14
scikit-learn==1.9.0
librosa==0.11.0
soundfile==0.14.0
torch==2.13.0
